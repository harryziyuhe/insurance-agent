# Copy this file to .env and fill in your own keys.
# .env is gitignored - it will never be committed.

# Free key: https://console.groq.com/keys (default LLM provider)
GROQ_API_KEY=

# Free key: https://aistudio.google.com/apikey (fallback provider if Groq is unavailable)
GEMINI_API_KEY=
